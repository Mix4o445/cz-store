services:
  mongo:
    image: mongo:7
    container_name: coolzone-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: coolzone

volumes:
  mongo_data:
